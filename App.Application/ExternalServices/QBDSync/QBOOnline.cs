using System;
using System.Collections.Generic;
using System.Text;

namespace App.Application.ExternalServices.QBDSync
{
    public class QBOOnline
    {
        public void TEST()
        {
            //var oauthClient = new OAuth2Client("clientId", "clientSecret", "redirectUri",Environment.Sandbox);
            // .Net OAuth2 Client gets the Discovery document as part of OAuth2Client object
            //string authUrl = oauthClient.GetAuthorizationURL(new List<OidcScopes> { OidcScopes.Accounting});

            //return Redirect(authUrl);
        }
    }
}
